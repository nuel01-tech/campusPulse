import { useState } from 'react';
import AppShell from '../components/AppShell';
import api from '../api/axios';
function Security(){const [currentPassword,setCurrent]=useState('');const [newPassword,setNew]=useState('');const [confirm,setConfirm]=useState('');const [message,setMessage]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();setMessage('');setError('');if(newPassword!==confirm){setError('New passwords do not match.');return}setLoading(true);try{await api.patch('/accounts/change-password/',{current_password:currentPassword,new_password:newPassword});setMessage('Password updated successfully.');setCurrent('');setNew('');setConfirm('')}catch(e){setError(e.response?.data?.detail||'Unable to update password.')}finally{setLoading(false)}};return <AppShell><div className="dashboard-head"><div><span className="eyebrow">Account security</span><h1>Security.</h1><p>Protect your CampusPulse account and keep your credentials private.</p></div></div>{(message||error)&&<div className={`notice ${message?'success':'error'}`}>{message||error}</div>}<section className="panel settings-section"><div className="panel-head"><div><span className="eyebrow">Password</span><h2>Change password</h2></div><span className="secure-label">● Protected</span></div><form className="form-grid one-column" onSubmit={submit}><label>Current password<input type="password" value={currentPassword} onChange={e=>setCurrent(e.target.value)} required autoComplete="current-password"/></label><label>New password<input type="password" value={newPassword} onChange={e=>setNew(e.target.value)} minLength={8} required autoComplete="new-password"/></label><label>Confirm new password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} minLength={8} required autoComplete="new-password"/></label><div className="form-actions"><button className="button primary" disabled={loading}>{loading?'Updating…':'Update password'}</button></div></form></section><section className="panel info-card security-note"><div className="info-icon">✓</div><div><strong>Security reminder</strong><p>Never share your password or class code. CampusPulse will never ask you to send your password through a message.</p></div></section></AppShell>}
export default Security;
