import { useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import { jwtDecode } from 'jwt-decode';
import api from '../api/axios';

function Login(){const [username,setUsername]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const navigate=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{const r=await api.post('/token/',{username,password});localStorage.setItem('access',r.data.access);localStorage.setItem('refresh',r.data.refresh);const d=jwtDecode(r.data.access);navigate(d.role==='CLASS_REP'?'/rep':'/student');}catch{setError('Invalid username or password.');}};return <div className="auth-page"><div className="auth-side"><div className="brand light-brand"><span className="brand-mark">CP</span><span>CampusPulse</span></div><div><span className="eyebrow light">University attendance, simplified.</span><h1>One clear place for the everyday campus routine.</h1><p>Check in, manage sessions and stay informed without the clutter.</p></div><span className="auth-side-foot">Olabisi Onabanjo University</span></div><div className="auth-main"><div className="auth-card"><div className="auth-mobile-brand"><div className="brand"><span className="brand-mark">CP</span><span>CampusPulse</span></div></div><span className="eyebrow">Welcome back</span><h2>Sign in to your workspace.</h2><p className="auth-sub">Use your CampusPulse account to continue.</p><form onSubmit={submit} className="stack-form"><label>Username<input value={username} onChange={e=>setUsername(e.target.value)} placeholder="Enter your username" autoComplete="username" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" autoComplete="current-password" required/></label><button className="button primary large">Sign in</button></form>{error&&<div className="notice error">{error}</div>}<p className="auth-switch">New to CampusPulse? <Link to="/signup">Create an account</Link></p></div></div></div>}
export default Login;
